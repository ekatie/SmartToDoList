# Routes

B  GET          /users              (not needed)
R  GET          /users/:id
E  POST         /users/:id
A  POST         /users
D  POST         /users/:id/delete   (not needed)

B  GET          /tasks
R  GET          /tasks/:id          (not needed)
E  POST         /tasks/:id
A  POST         /tasks
D  POST         /tasks/:id/delete